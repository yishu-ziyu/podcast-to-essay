# 0004 资料库改为顶栏按钮 + 左侧浮层

- 日期：2026-09-25（提交 `3dc812b`）
- 状态：采用

## 背景
58px 窄栏只放一个菜单按钮；展开时推动整页；手机上与顶栏叠成两层共 110px。首页已有「最近」后，资料库变成「全部条目」的次要入口。

## 决定
去掉窄栏，按钮放进顶栏最左；点开从左侧滑出浮层盖在内容上，点外面、Esc、✕ 关闭并把焦点还给按钮。

## 放弃的选项
- 桌面常驻侧栏（类 ChatGPT）：条目少时浪费阅读宽度。
- 保留窄栏加图标：改动小但仍占一列。

## 后果
切换条目多一次点击；条目很多时需要再考虑搜索。
