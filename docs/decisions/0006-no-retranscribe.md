# 0006 素材页不提供「重新转录」

- 日期：2026-09-25（提交 `cb788fa`）
- 状态：采用

## 背景
已转完的条目直接再转录，`transcribe.mjs` 会复用全部分段结果，什么都不变；要真正重转得先清缓存，而同一音频、同一模型再识别一次结果基本相同，却多花一次额度。

## 决定
阅读器「素材」页只展示来源、原链接、时长、分段、识别与整理模型。

## 放弃的选项
- 清缓存后真正重转：将来换了更好的识别模型时有用，届时再加。

## 后果
想重转只能更换音轨（会清缓存，见 0002）。
