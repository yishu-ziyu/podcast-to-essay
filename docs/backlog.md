# 问题清单

用产品时发现的问题、已知缺口和待办。随口告诉 Agent 即可，由它补全这里的记录；修好后把状态改为「已修」并写上提交号，保留一段时间再清理。

优先级：**P0** 数据错误或主流程走不通 · **P1** 影响使用、需要尽快处理 · **P2** 体验与打磨。

## 使用中发现

| 日期 | 现象 | 怎么触发 | 优先级 | 状态 |
|---|---|---|---|---|
| | | | | |

## 已知缺口

| 问题 | 影响 | 优先级 | 状态 |
|---|---|---|---|
| 新导入的条目没有时长（上传与链接导入都不写 `duration`） | 资料库和素材页只显示日期 | P1 | 已修（B1，见提交）；此前导入的两条仍无时长 |
| 现有 3 篇文章没有段落映射 `paraMap` | 核对时两栏不能互相定位。映射丢失的根因已修（见 [e2e 记录](evals/2026-09-25-e2e-real-links.md)），对每篇「重新整理」一次即可生成（耗整理额度） | P1 | 待你决定 |
| 「誊清」商标未查（第 9、42 类） | 注册或公开推广前需确认 | P1 | 待你查 |
| 同一链接可以重复导入，没有提醒，会重新完整下载 | 资料库出现重复条目、浪费时间 | P2 | 待办 |
| 一段完全无人声（如片头音乐）的 3 分钟分块识别为空时，整次转录失败 | 带长片头音乐的节目可能转不了 | P2 | 待验证 |
| 段落映射靠「条数 = 行数」推断按段合并；条数碰巧相等时会错配而不报警。根治是让模型按段落编号给区间 | 核对定位偶尔可能对错段 | P2 | 待办（需真实模型对比验证） |
| 所有者会话存在内存 | 每次部署或重启后要重新登录 | P2 | 待办 |
| 线上域名仍是 `lcw`（录成文缩写） | 与新名字不一致 | P2 | 暂不改 |
| 服务器连 GitHub 不稳定 | 部署时 `git pull` 常超时，已有 bundle 兜底 | P2 | 已有绕行 |

## 界面打磨（第三批）

| 问题 | 优先级 | 状态 |
|---|---|---|
| 分段稿直接显示原始 SRT，序号与时间码裸露 | P2 | 待办 |
| 转录进行中下方挂着灰色「第 3 步」卡片，占位多 | P2 | 待办 |
| 大屏首页「最近」下方大片留白 | P2 | 待办 |
| 条目多了以后资料库没有搜索 | P2 | 待办 |
| 复制按钮在非 https 环境下静默失败 | P2 | 待办 |
| 工作台「处理日志」从未被写入（死代码） | P2 | 待办 |
| Electron 桌面壳近期未验证 | P2 | 待验证 |
