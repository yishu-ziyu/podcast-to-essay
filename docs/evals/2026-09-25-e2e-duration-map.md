# 2026-09-25 端到端：条目时长、段落映射、线上部署

- 版本：`665aeeb` 加上本次改动；环境：本地（前端 :5190，服务端 :8790），游客模式另起临时服务（:5191 / :8791，临时数据目录，`ACCESS_PASSWORD` 已设）；线上 `lcw.yishuziyu.cn`
- 方式：Playwright 驱动真实界面，不拦截请求；转录与整理真实调用 StepFun

## 线上部署 `665aeeb`（游客）

| 输入 | 期望 | 实际 | 结论 |
|---|---|---|---|
| 部署前 `/data/jobs` | 没有 queued / running / paused | 3 个任务都是 succeeded | 通过 |
| `/api/health`、首页、`/assets/*` | 200，标题「誊清」 | 200，`index-8Rfq7EQI.js` | 通过 |
| 抖音分享口令（失效短链） | 说明链接失效 | 「这个分享链接没有打开具体视频，可能已失效或没复制完整…」 | 通过 |
| `example.com` | 拒绝 | 「这个页面不能直接导入…」，没有生成条目 | 通过 |

## 条目时长（B1）

| 输入 | 身份 | 期望 | 实际 | 结论 |
|---|---|---|---|---|
| 本地文件「播客试音.m4a」（38.6 秒） | 所有者 | 资料库、工作台、素材页、首页最近卡片都显示时长 | `00:00:38`，四处都有 | 通过 |
| 同一文件 | 游客 | 资料库、工作台显示时长 | `00:00:38` | 通过 |
| YouTube `watch?v=jNQXAC9IVRw`（19 秒） | 所有者 | 显示时长 | 13 秒导入，`00:00:19` | 通过 |
| B 站 `BV1darmBcE4A`（2 小时） | 所有者 | 显示时长 | 178 秒导入，`02:02:44`（yt-dlp 标 `02:02:45`；按抽出的音频向下取整，差不到 1 秒） | 通过 |

测试条目都已删除（游客没有删除入口，临时数据目录整个删掉）。

## 段落映射（B2）与现有 3 篇重新整理（A2）

打分：用字符二元组找出逐字稿里与每个自然段最像的块，看模型给的区间是否包含它。跨度 = 区间平均覆盖几块。

| 逐字稿 | 旧方案：段落 / 覆盖 / 命中 / 跨度 | 新方案：段落 / 覆盖 / 命中 / 跨度 |
|---|---|---|
| 腾讯阿里 AI 办公（5 块） | 3 / 100% / 100% / 2.0（分段丢失）；另一次 12 / 100% / 67% / 0.8 | 21 / 100% / 95% / 1.1 |
| 课代表立正（5 块） | 6 / 0%（映射作废） | 12 / 100% / 100% / 1.08 |
| 翁家翌访谈（41 块） | 14 / 100% / 43% / 2.9 | 18 / 100% / 78% / 2.6 |

- 新方案下 3 篇都能在核对视图点段落、分段稿同步高亮；人工抽查腾讯那篇「受 Claude Code 的带动…」一段，高亮的是讲 Claude Code 的段 04，正确。
- 38 秒测试音频走完整流程：上传 → 转录 6 秒 → 整理 8 秒（4 段）→ 核对，点最后一段，分段稿同步高亮。

## 发现的问题

1. 整理达到长度上限失败（18 分钟节目就会）：step-3.7-flash 先在推理里完整起草一遍，推理计入 `max_tokens`（一次调用：输出 11318 token，其中正文 3659 字、推理 1.6 万字）→ 已修，上限 12k → 100k。
2. 模型有时只用单换行分段，整节被当成一段，阅读页成一大块、核对只能定位整节 → 已修，`normalizeArticle` 按行拆段。
3. 旧映射命中率低、会整篇作废，秒数多为插值 → 已修，见 [0009](../decisions/0009-chunk-tag-map.md)。
4. 文章跳过节目开头的新闻速览（腾讯阿里那期，新旧版本都没有拼多多、小米等几条）→ 记入 backlog，待判断。
5. 一次整理请求 5 秒内报「网络连不上外部服务」，重试即成功；提示文字正确。

## 额度

整理约 10 次（1 次撞长度上限失败、1 次网络失败、1 次直接调接口复现、旧方案 3 篇、新方案 3 篇、测试音频 1 篇）；转录 1 块（38 秒）。
