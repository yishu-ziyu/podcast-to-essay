# 2026-09-25 端到端：真实链接导入与完整主流程

- 版本：`53552db` 起步，修复随本次提交；环境：本地（前端 :5190，服务端 :8790）；身份：所有者
- 方式：Playwright 驱动真实界面，粘贴真实链接、上传真实文件，不拦截请求；转录与整理真实调用 StepFun

## 链接导入

| 输入 | 期望 | 实际 | 结论 |
|---|---|---|---|
| YouTube `watch?v=jNQXAC9IVRw`（19 秒） | 导入成功 | 10 秒成功，标题「Me at the zoo」 | 通过 |
| `youtu.be/jNQXAC9IVRw` | 导入成功 | 3 秒成功 | 通过 |
| B 站 `BV1darmBcE4A`（2 小时） | 导入成功，有进度 | 173 秒成功，下载百分比持续更新 | 通过 |
| 抖音分享口令，内含失效短链 `v.douyin.com/abc123xyz/` | 说明链接失效 | 修复前：「这是抖音主页」；修复后：「这个分享链接没有打开具体视频，可能已失效或没复制完整…」 | 已修 |
| B 站个人主页 `space.bilibili.com/1` | 拒绝并说明 | 「这是 B 站页面，不是具体视频…」 | 通过 |
| 普通网页 `example.com` | 拒绝并说明 | 「这个页面不能直接导入…」 | 通过 |
| 内网 `127.0.0.1:8080/a.mp3` | 拒绝 | 「不收本机链接」 | 通过 |
| 不带协议 `bilibili.com/video/…` | 输入时提示 | 「链接需以 http:// 或 https:// 开头」 | 通过 |

## 完整主流程（本地文件「播客试音.m4a」，38.6 秒中文语音）

| 步骤 | 实际 | 结论 |
|---|---|---|
| 上传 | 1 秒，中文文件名保留为标题 | 通过 |
| 转录（第一次） | 5 秒后失败，界面显示英文原文「fetch failed」 | 已修：逐段自动重试，最终失败时给中文说明 |
| 转录（重跑） | 21 秒完成，识别模型写入 `asr-meta.json` | 通过 |
| 整理成文 | 18 秒，2 个小标题、忠于原文 | 通过 |
| 核对 | 修复前：新文章也没有段落映射；修复后：点段落，分段稿同步高亮 | 已修 |
| 素材页 | 来源、文件名、识别、整理均正确；整理日期修复前按 UTC 显示成前一天 | 已修 |
| 改名 | 界面与服务端都更新 | 通过 |
| 删除 | 确认框 → 已删除，条目消失 | 通过 |

## 发现的问题

1. 抖音失效短链被说成「抖音主页」→ 已修，`services/media-ingest.mjs`。
2. 单段识别一次网络抖动就让整次转录失败，且提示是「fetch failed」→ 已修：网络与服务端错误每段重试 2 次（2 秒、5 秒），额度与授权错误不重试。测试 `transcribe-retry.test.mjs`。
3. 模型按「行」给段落时间区间，与按空行分段的数量对不上，映射被整体丢弃，**所有文章的核对定位都不可用** → 已修：条数等于总行数时按段合并。测试见 `article.test.mjs`。
4. 转录失败后重试成功，条目仍显示「转录失败」→ 已修：只看最近一次转录任务，已有初稿不再显示失败。测试 `episode-state.test.mjs`。
5. 素材页整理日期按 UTC 显示 → 已修，改为本地日期。
6. 同一链接可重复导入，无提醒，重新完整下载 → 记入 backlog。
7. 新导入条目仍无时长 → 已在 backlog。

测试条目已在界面里删除；原有 3 篇未受影响。
